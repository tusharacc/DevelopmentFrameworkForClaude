# BUG-017 — simplify.md edge case: `.TS` typo should be `.ts`

**Severity:** low
**Status:** open
**Workspace:** code-quality-agents
**Created:** 2026-05-02

## Description

In `skills/code-quality/agents/simplify.md`, the edge case bullet reads:
> `No .py/.js/.TS files in diff`

`.TS` should be `.ts` (lowercase). The inconsistency could cause confusion about whether `.TS` is intentional (Windows case-insensitive filesystem artifact) or a typo.

## Fix

Change `.TS` to `.ts` on line 99 of `skills/code-quality/agents/simplify.md`.
